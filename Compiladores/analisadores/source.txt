# Codigo de exemplo da linguagem ficticia

int primeiroNumero = 5;
int segundoNumero = 5;
int soma = primeiroNumero + segundoNumero;

bool verificador;
if (soma == 10):
    verificador = true;
else:
    verificador = false;
